# WorkRight development and learning roadmap

## Current status: Phase 0 — first endpoint verified

- [x] Inspect the existing local Git repository.
- [x] Add the project README and ignore rules.
- [x] Create empty backend, frontend, knowledge, and evaluation folders.
- [x] Preserve the supplied proposal as a reference document.
- [x] Record the local-model direction and RAG learning guide.
- [x] Inspect installed development tools and explain the inventory.
- [x] Defer OrbStack and Docker for now.
- [x] Create a project-local `.venv` with installed Python 3.12.14; record Python 3.12 in `.python-version`.
- [x] Create `pyproject.toml`, add FastAPI and Uvicorn to `.venv`, and generate `uv.lock`.
- [x] Explain and implement the first health endpoint: `backend/main.py` serves `GET /health` via Uvicorn and returned `{"status":"ok"}`.
  - Learned: a virtual environment stores absolute paths, so moving the project folder broke `.venv/bin/uvicorn` (`bad interpreter: .../Documents/ChatGPT/WorkRight/...`). The interpreter symlink survived, but console scripts kept the old shebang. Fix: delete `.venv` and run `uv sync --locked`; `uv sync` alone left the broken scripts untouched because installed versions still matched the lockfile.
  - Learned: FastAPI defines routes and turns them into JSON; Uvicorn is the server process that listens on a port and forwards requests to the app. `backend.main:app` tells Uvicorn "import module `main` in package `backend`, use the object named `app`".

- [x] Chose native PostgreSQL over Docker and installed it: `brew install postgresql@17 pgvector` (PostgreSQL 17.11, pgvector 0.8.6), started as a auto-restarting service, created the `workright` database, and enabled `CREATE EXTENSION vector;`.
  - Learned: Homebrew installs system-wide in `/opt/homebrew` (shared by all projects), unlike the project-local `.venv`; project isolation for databases happens inside the server as separate named databases. All seven PostgreSQL dependencies were already present from Homebrew Python, so only ~350 MB was added. The formula is keg-only: binaries are reached via `/opt/homebrew/opt/postgresql@17/bin/`, and `pg_config` there is shadowed by an Anaconda copy on PATH (noted as a future debugging lead).
- [x] Installed SQLAlchemy 2.0.52, Alembic 1.20.0, and psycopg 3.3.5 (binary variant) with `uv add`, and upgraded `GET /health` to check database connectivity. Verified both outcomes: live database returns `{"status":"ok","database":"up","postgres":"17.11 (Homebrew)","pgvector":"0.8.6"}`; an unreachable database returns `{"status":"degraded","database":"down"}` with no error details leaked.
  - Learned: `psycopg` is the driver that actually speaks PostgreSQL's wire protocol; SQLAlchemy's URL scheme `postgresql+psycopg://` selects it. The `[binary]` extra bundles its own libpq, dodging the Anaconda `pg_config` PATH shadow recorded earlier. `create_engine()` opens no connection — it just stores config; connections are made per request and released. Homebrew Postgres uses trust auth on localhost, so the default user is the macOS username with no password; `DATABASE_URL` environment variable overrides for other machines.
- [x] Created the first `Employee` model (`backend/models.py`, connection shared via `backend/database.py`), wired Alembic (`alembic init` + `env.py` reusing the app's `DATABASE_URL` and `Base.metadata`), autogenerated the first migration, reviewed it, and applied it: the live `employees` table was inspected with `\d` and its constraints exercised in rolled-back/cleaned psql tests.
  - Learned — the review-before-apply caught two real bugs: (1) SQLAlchemy's `Enum(PyEnum)` stores member **names** by default, so the CHECK constraint was generated as `('EMPLOYEE','MANAGER',...)` instead of our snake_case values; fixed with `values_callable=lambda e: [m.value for m in e]`. (2) An ORM `default=` is invisible to the database — a raw-SQL insert violated NOT NULL; fixed by adding `server_default` to `role` and `employment_status`. Also learned: regenerating a migration discards hand-edits (the `updated_at` trigger had to be re-added), and `now()` is frozen per transaction, so trigger testing must span two transactions.
- [x] Seeded six synthetic employees (`backend/seed.py`) through the ORM session — first use of the model by application code. Rows chosen to cover every branch: <2-year newbie, 5–10yr tier, 10yr+ tier manager (all five others' `manager_id` target), part-timer (pro-rata), contract, and a Sabah employee (escalation path). Verified snake_case enum values and self-referencing links from psql; script is idempotent.
- [ ] Install and verify Ollama with a local model response (final Phase 0 item).

Next step: Ollama installation + one verified local model completion, completing Phase 0; then Phase 1 begins with the first agent tool (`get_employee` / leave-tier calculation over this seed data).

At preparation time, the repository is on `main`, has no commits, and has no configured remote. A hosted Git repository has not been created. The remaining steps below are a roadmap, not instructions to execute them without the user's next learning session.

## Phase 0 — Environment and foundation (started)

Learn what each installed tool does before using it.

- Check Python, uv, Node.js, npm, PostgreSQL/Docker, and Ollama availability.
- Choose native PostgreSQL or a database-only Docker Compose setup.
- Explain virtual environments, dependency lockfiles, environment variables, and migrations.
- Install the chosen dependencies and models when ready.
- Build the initial backend and connect it to PostgreSQL.
- Create and inspect synthetic employee records.
- Verify a local model response.

Completion: a working local backend, database connection, employee lookup, and Ollama connection.

## Phase 1 — Agent loop and employee tools

Progress (2026-09-15): first tools exist as plain, tested Python —
`backend/tools.py` (`get_employee`, `completed_years`, `statutory_annual_days`,
`annual_leave_entitlement`) with `backend/test_tools.py` (11 pytest cases:
anniversary-boundary math, s.60E tiers 8/8/12/16, all six seeded employees,
escalation results proven to carry NO computed number, not-found handling).
pytest configured via pyproject (`pythonpath = ["."]`). The tool contract
(status: ok | escalate | not_found) is the guard rail the LLM loop will later
be built around. `search_policy` tool added (employee-no input — jurisdiction
read from the DB, never accepted as an argument; verbatim texts + citations;
empty result → nothing_applicable → escalate) and `backend/permissions.py`
enforces handbook §9 access control: own data / direct reports (manager) /
all (hr, admin), with a `forbidden` status carrying no target data; HR
employee WR-0007 seeded for the full matrix (22 pytest cases green).
Next: the manual tool-calling loop with Qwen3 (menu, JSON deal, validation,
session-injected caller, 8-round cap).

**Phase 1 COMPLETE (2026-09-15):** `backend/agent.py` — a manual loop over
Ollama-native tool calls with qwen3:4b: menu of 3 tools (caller identity
never advertised, defensively popped from model args), per-request validation
(unknown tool / missing args → rejection-as-data), session-injected caller,
8-round cap ending in forced escalation, and a full trace per run. 28 pytest
cases green (doorman tests need no LLM). End-to-end demo: multi-tool answer
with correct citations (18 days HB-004, statutory 12 from LAW-003, half-days
from §4.2); Sabah employee correctly escalated with NO number invented;
self-approval request refused (no such tool exists). Phase 1 completion
criterion met: employees receive answers grounded in database facts.

Learn how a model requests a tool and how application code executes it.

- Implement the explicit agent loop with bounded iterations.
- Add employee profile, leave balance, and service calculation tools.
- Validate arguments and enforce employee access in backend code.
- Inspect tool calls and results.

Completion: an employee can ask about their leave balance and receive an answer based on database facts.

## Phase 2 — Local RAG and vector search

Progress (2026-09-14): structure-aware chunker (`backend/chunking.py`) produces
18 inspectable chunks; Ollama + BGE-M3 pulled locally; migration #2 created
`policy_documents` + `policy_chunks` (vector(1024)); loader
(`backend/load_chunks.py`) embedded and upserted all 18 chunks;
`backend/search_demo.py` performs LLM-free retrieval and returns correct chunks
for English and Malay questions.
**Same day:** LAW-003's s.60E tier error corrected in the source (8/8/12/16,
boundary at 10 years, dated correction note in the chunk) and re-embedded;
migration #3 added the user-approved 12-field chunk metadata (chunk_id,
document_id, source_type, topic, subtopic, section, jurisdiction TEXT[],
effective_from, effective_to, version, authority, source_url) with a
value-preserving rename review catch (autogenerate would have dropped and
re-added chunk_no, destroying 18 IDs); migration round-trip tested up→down→up;
loader backfills metadata from explicit per-chunk maps + document masters and
runs a drift audit (0 violations); search now filters jurisdiction + as-of date
— demo: same question from a Peninsular employee returns law+policy chunks with
citations, from a Sabah employee returns nothing applicable → escalate to HR.
Pending: verify corpus against official JTKSM text; labelled evaluation
question set; topic/subtopic routing in the agent loop.

Follow the [RAG learning guide](RAG_LEARNING_GUIDE.md) through small exercises.

- Begin with one fictional company-policy document.
- Inspect extracted text and chunk boundaries.
- Generate and inspect local embeddings.
- Store vectors and passages in PostgreSQL.
- Search without an answering LLM first.
- Add source metadata, applicability filters, grounded answers, and citations.
- Add verified public legal sources.
- Evaluate English/Malay retrieval and tune against a labelled question set.

Completion: policy answers use identifiable, applicable source passages, with missing-evidence cases handled explicitly.

## Phase 3 — Leave requests

**Progress (2026-09-17):** `leave_requests` ledger created (migration
`404e9eeb0501`, round-trip tested up→down→up) with all five CHECK constraints
(separate legal categories, date order, positive days, half-day-only-single-day,
closed status vocabulary), `status` defaulting to `pending_manager`, and the
partial unique index `uq_leave_no_active_duplicates` — adversarially verified:
duplicate live request blocked, corrected retry after rejection allowed,
`marriage` type and two-day half-day rejected, updated_at trigger fires across
transactions, no test rows left behind.
**Steps 1–2 done (2026-09-17):** balance wiring — `annual_leave_entitlement`
now reports entitled/approved/pending/remaining/available (available =
entitled − approved − pending; pending holds days until a human decides).
`submit_leave_request` tool (INSERT-only, routes annual→pending_manager and
sick/hospitalisation→pending_hr, validates scope and balance, computes
deductible days itself — weekends excluded per LAW-004, half-days via
day_portion, duplicate attempts rejected by the DB index, SpoofGuard tests
prove the model cannot borrow another identity even on the write path).
Menu grew a `required`-args mechanism for optional parameters. 45 pytest
cases green; ledger verified empty after the suite. Remaining: e2e demo
(agent books Christmas leave → inspect the pending row in psql).

Learn how model-selected actions remain constrained by ordinary software rules.

- Define working-day and holiday calendar behaviour.
- Add annual, sick, and hospitalisation leave workflows.
- Validate balances and keep leave categories separate.
- Prevent duplicate creation and create pending requests.
- Apply relevant jurisdiction and authorization checks alongside the actions.

Completion: valid requests enter the approval workflow; invalid or unsupported requests receive the appropriate outcome.

## Phase 4 — Approvals, flexible work, and persistent state

**Approvals done (2026-09-17):** `approvals` audit table (migration
`3c11594bea19`, round-trip + adversarial verified: unique per request+level,
closed level/decision vocabularies, CASCADE cleanup, no orphans) plus
`permissions.can_decide_leave` (direct manager for manager-level; HR/admin for
either level; self-decision never allowed) and the `decide_leave_request` tool
on the menu — the agent records decisions only for an authorized human caller.
57 pytest cases green. Live demo: peer attempt → forbidden; Siti approved
`LV-2026-0001` with a full audit row (level=manager, approver, requested_at,
decided_at); balance moved pending 2→0, approved 0→2, remaining 18→16.
**Flexible work done (2026-09-17):** `flexible_work_requests` table
(migration `a8e02af4d7bd`) with multi-dimension booleans (hours/days/place,
at-least-one enforced in DB and tool), two code-computed deadline clocks
(30-day company target per HB §7.5, 60-day statutory per LAW-008 s.60Q)
stored as snapshots with provenance FK to the policy document;
`approvals` extended with `fwa_request_id` + exactly-one-target CHECK
(autogenerate MISSED this check — caught in review, hand-added);
`submit_fwa_request` and unified `decide_request` (LV-/FW- routing) tools;
two-stage flow — manager approval advances to `pending_hr`, HR finalizes;
rejections require reasons (§7.6). 72 pytest cases green. Live demo:
Wei Jie's WFH request created with agent-flagged deadline note → manager
approved (advanced) → HR approved (final), two audit rows with names.
Remaining: persistent agent cases with restart recovery.

Learn the difference between conversation history and durable task state.

- Add manager/HR decisions with backend-enforced roles.
- Collect and store written flexible-work requests.
- Save waiting states and resume cases from decision events.
- Verify restart recovery and duplicate-event handling.
- Show outcomes through local application notifications/status.

Completion: the same case can pause, survive a backend restart, and resume after an authorized decision.

## Phase 5 — Evaluation and reliability

Learn to separate retrieval failures, model failures, and application failures.

- Run repeatable policy, tool, state, and authorization scenarios.
- Check missing information, unsupported scope, and prompt-injection attempts.
- Inspect execution traces, citations, latency, and memory usage.
- Improve components only where measurements show a problem.

Completion: documented results demonstrate the supported workflows and identify remaining limitations.

## Phase 6 — Interface

**Login system + React app landed (2026-09-17):** `employees.password_hash`
(bcrypt) + `sessions` table (migration `d124367b5ba3`; session tokens stored
as SHA-256 digests, 7-day expiry, revocable); `backend/auth.py` + `/auth/
login|logout|me` endpoints; **`/chat` no longer accepts an employee_no in the
body — identity comes from the session cookie only** (the documented V1
trust placeholder is closed). Demo password `workright123` for all fictional
employees, seeded via `seed.set_demo_passwords()`. 80 pytest cases green.
Frontend: React + Vite + TypeScript app in `frontend/` — login page with a
one-click demo cast, chat page showing the logged-in identity, answers with
a collapsible tool-call trace, suggestion chips, session-expiry handling.
Vite proxies /auth + /chat + /health to :8000 (same-origin cookies, no CORS).
Run: `uv run uvicorn backend.main:app --port 8000` + `cd frontend && npm run
dev` → http://localhost:5173. Next: request-history panel, manager inbox.

Started early (2026-09-17): `POST /chat` added to the FastAPI app —
`{"message", "employee_no"}` in, `{answer, trace, rounds}` out, with the
no-login trust note documented in code (Layer 1 of the interface: the door
every future UI will use). Verified over HTTP via curl: balance answer
(16 available, reflecting the live ledger), Sabah escalation, and — first
live firing of the forbidden path — Danial asking for Siti's balance gets
refused by the backend with the model relaying it faithfully. Visual chat
UI (React + Vite) remains deferred until the backend stabilises.

Learn how the frontend presents backend facts and workflow state.

- Add employee chat, balances, request history, and source references.
- Add manager/HR approval screens.
- Display pending and final outcomes clearly.
- Demonstrate the full workflow through the browser.

Completion: a usable local demonstration of employee requests, evidence, approvals, and outcomes.

## Working agreement

We will explain, implement, inspect, and review one small step at a time. Tests and permission checks belong with the features they protect; they are not postponed until Phase 5. No application implementation is included in the repository-preparation step.
